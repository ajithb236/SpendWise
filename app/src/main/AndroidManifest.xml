<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SMS Related Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    
    <!-- Notification Permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Internet and Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- File and Storage (for backup if needed) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".SpendWiseApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpendWise">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SMS Broadcast Receiver -->
        <receiver
            android:name=".receivers.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Dialog Activities -->
        <activity
            android:name=".dialogs.AddTransactionActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.AddAccountActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.TransactionDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.AccountDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.EditInstrumentActivity"
            android:exported="false"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.MerchantAliasesActivity"
            android:exported="false"
            android:theme="@style/Theme.SpendWise" />

        <activity
            android:name=".dialogs.EditTransactionActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SpendWise" />

    </application>

</manifest>
